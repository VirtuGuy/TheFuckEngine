import funkin.graphics.shader.FunkinShader;
import funkin.play.stage.Stage;
import funkin.util.FileUtil;

class SchoolEvilStage extends Stage
{
    var wiggleTime:Float = 0;
    var wiggle:FunkinShader;

    public function new()
    {
        super('school-evil');
    }

    override function onCreate(event:ScriptEvent)
    {
        super.onCreate(event);

        wiggle = new FunkinShader('wiggle');
        wiggle.setFloat('speed', 2);
        wiggle.setFloat('frequency', 10);
        wiggle.setFloat('waveAmplitude', 0.03);

        getProp('school').shader = wiggle;
        getProp('ground').shader = wiggle;
    }

    override function onUpdate(event:UpdateScriptEvent)
    {
        super.onUpdate(event);

        wiggleTime += event.elapsed;

        wiggle.setFloat('time', wiggleTime);
    }
}
